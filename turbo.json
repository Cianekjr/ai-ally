{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    "**/.env",
    ".env",
    ".env.local",
    "tsconfig.json"
  ],
  "pipeline": {
    "lint": {
      "outputs": []
    },
    "dev": {
      "dependsOn": [
        "^generate"
      ],
      "cache": false
    },
    "build": {
      "dependsOn": [
        "^build",
        "^generate"
      ],
      "outputs": [
        "dist/**",
        ".next/**",
        "!.next/cache/**"
      ]
    },
    "build#server": {
      "env": [
        "PORT",
        "JWT_SECRET",
        "DATABASE_URL",
        "MAILGUN_API_KEY"
      ]
    },
    "build#client": {
      "env": [
        "PORT",
        "APP_PUBLIC_URL",
        "NEXT_PUBLIC_API_URL"
      ]
    },
    "generate": {
      "cache": false
    },
    "push": {
      "cache": false
    }
  }
}